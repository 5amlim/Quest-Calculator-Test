Lab Collection Calculator v10.32

Upload every file in this folder to the root of your GitHub repository.
Replace files with the same names, commit to the main branch, and GitHub Pages will republish automatically.

The built-in catalog loads from data.js. Browser storage contains only user-created tests/overrides and selected test IDs, preserving the v10.30 quota-safe architecture.
